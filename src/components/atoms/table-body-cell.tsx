import { styled, TableCell } from '@mui/material';

export const StyledDataCell = styled(TableCell)(({ theme }) => ({
  paddingX: '10px',
  whiteSpace: 'nowrap',
  fontSize: '0.7rem',
  textAlign: 'center',
}));
